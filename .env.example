# Copy this to .env and fill in your values
# NEVER commit .env — it's in .gitignore

# Optional: override the default prompt used in tests
WHISK_PROMPT=A majestic lion portrait with a golden crown, oil painting style, hyper realistic

# Set to "true" to record videos for all tests (not just failures)
RECORD=false
